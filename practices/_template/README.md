# Practice Template

Copy this directory to create a new practice module:

```bash
cp -r practices/_template practices/your_practice_name
```

Then:
1. Update `config.yaml` with your practice details
2. Add agent classes in `agents/`
3. Add prompt templates in `prompts/`
4. Add deliverable templates in `templates/`
5. Register the practice in the root README
